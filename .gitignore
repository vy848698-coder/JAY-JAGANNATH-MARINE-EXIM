# ── Vercel ──
.vercel

# ── Node ──
node_modules/
npm-debug.log*
yarn-error.log*
.pnpm-debug.log*

# ── Environment ──
.env
.env.local
.env.*.local

# ── Mail credentials and received enquiries ──
# api/mail-config.php holds the Gmail app password; the example stays tracked.
api/mail-config.php
api/storage/

# ── Editors ──
.vscode/*
!.vscode/extensions.json
.idea/
*.swp

# ── OS ──
.DS_Store
Thumbs.db
desktop.ini

# ── Build output ──
dist/
build/
.cache/
